import React from 'react';

const SurahBtn = (surahs) => {
    const {surah} = surahs;
    // console.log(surah.name);
    return (
        <div>
            
        </div>
    );
};

export default SurahBtn;